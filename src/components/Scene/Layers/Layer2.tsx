import React from 'react';
// import { useGLTF } from '@react-three/drei';

export default function Layer2() {
  // Aquí se cargarán los modelos GLB de la escena usando useGLTF
  // Ejemplo: const { scene } = useGLTF('/modelos/escenario.glb');

  return (
    <group name="layer-2">
      {/* <primitive object={scene} /> */}
    </group>
  );
}
